# Single requirements file — covers CLI, GUI (PyQt6), online-fix (Selenium), and Tor fallback.
# Versions taken from requirements-consumer.txt (more recent); gui + online-fix merged in.
#
# ---------------------------------------------------------------
# INSTALLATION (two commands required due to steam urllib3 conflict)
#
#   Step 1:  pip install -r requirements.txt
#   Step 2:  pip install steam==1.4.4 --no-deps
#
# Why? steam==1.4.4 metadata declares urllib3<2, but selenium 4.x
# needs urllib3>=2.x. Steam only uses `requests` at runtime and works
# fine with urllib3 2.x — the constraint is just stale. Installing
# steam with --no-deps bypasses the check; all its deps are already
# installed by Step 1.
# ---------------------------------------------------------------
#
# ---------------------------------------------------------------
# OPTIONAL: Tor Expert Bundle  (not a Python package, not via pip)
#
# Used as automatic fallback when the primary GMRC endpoint
# (st-gmrc.kur0.deno.dev) is unreachable. Without it the Tor path
# is silently skipped and you fall back to ManifestHub API or
# manual code entry.
#
# Easiest way: run install_online_fix_requirements.bat — it will
# offer to open the Tor download page automatically after pip install.
# Or manually:
#   1. https://www.torproject.org/download/#tor-downloads
#   2. Download "Expert Bundle" (no browser, just the daemon)
#   3. Unzip anywhere, run tor.exe once — it listens on :9050
#   4. Leave it running while using the tool; no other config needed
#
# pysocks (already listed below) handles the SOCKS5 connection.
# ---------------------------------------------------------------
anyio==4.12.1
    # via httpx
attrs==25.4.0
    # via
    #   outcome
    #   trio
backports-tarfile==1.2.0
    # via jaraco-context
beautifulsoup4==4.12.3
    # via steam-manifest-decrypt (pyproject.toml)
cachetools==6.2.6
    # via steam
certifi==2026.2.25
    # via
    #   httpcore
    #   httpx
    #   requests
    #   selenium
cffi==2.0.0
    # via
    #   gevent
    #   pynacl
    #   trio
charset-normalizer==3.4.4
    # via requests
colorama==0.4.6
    # via
    #   steam-manifest-decrypt (pyproject.toml)
    #   click
    #   tqdm
configupdater==3.2
    # via steam-manifest-decrypt (pyproject.toml)
exceptiongroup==1.3.1
    # via
    #   anyio
    #   trio
    #   trio-websocket
gevent==25.9.1
    # via
    #   gevent-eventemitter
    #   steam
gevent-eventemitter==2.1
    # via
    #   steam-manifest-decrypt (pyproject.toml)
    #   steam
greenlet==3.2.4
    # via gevent
    # NOTE: 3.2.5 has no pre-built wheel for Python 3.12 on Windows;
    #       pinned to 3.2.4 which ships a cp312 wheel and installs without MSVC.
h11==0.16.0
    # via
    #   httpcore
    #   wsproto
httpcore==1.0.9
    # via httpx
httpx==0.28.1
    # via steam-manifest-decrypt (pyproject.toml)
idna>=3.10
    # via
    #   anyio
    #   httpx
    #   requests
    #   trio
    # NOTE: was pinned to 3.11 but seleniumbase locks idna==3.10 on
    # newer releases. Both 3.10 and 3.11 work for us; loosen so the
    # CI workflow's pip install resolves cleanly with seleniumbase.
importlib-metadata==8.7.1
    # via
    #   keyring
inquirerpy @ git+https://github.com/kazhala/InquirerPy@714d9068896c1a1f9f4d1354f43f922cd5cfe16d
    # via steam-manifest-decrypt (pyproject.toml)
jaraco-classes==3.4.0
    # via
    #   keyring
    #   keyrings-alt
jaraco-context==6.1.0
    # via
    #   keyring
    #   keyrings-alt
jaraco-functools==4.4.0
    # via keyring
keyring==25.7.0
    # via steam-manifest-decrypt (pyproject.toml)
keyrings-alt==5.0.2
    # via steam-manifest-decrypt (pyproject.toml)
lxml==6.0.2
    # via steam-manifest-decrypt (pyproject.toml)
markdown-it-py==3.0.0
    # via rich
mdurl==0.1.2
    # via markdown-it-py
more-itertools==10.8.0
    # via
    #   jaraco-classes
    #   jaraco-functools
msgpack==1.1.2
    # via steam-manifest-decrypt (pyproject.toml)
outcome==1.3.0.post0
    # via
    #   trio
    #   trio-websocket
packaging>=26.0
    # via
    #   black
    # NOTE: was pinned to 26.0 but seleniumbase >=4.48 needs packaging>=26.2.
    # Allow newer to unblock CI; both work for our pyinstaller / black flow.
pathvalidate==3.3.1
    # via steam-manifest-decrypt (pyproject.toml)
pfzy==0.3.4
    # via inquirerpy
Pillow==11.2.1
    # via steam-manifest-decrypt (pyproject.toml)
prompt-toolkit==3.0.52
    # via inquirerpy
protobuf==3.20.3
    # via
    #   steam-manifest-decrypt (pyproject.toml)
    #   steam
psutil==7.2.2
    # via steam-manifest-decrypt (pyproject.toml)
pycparser==2.23
    # via cffi
pycryptodome==3.23.0
    # via steam-manifest-decrypt (pyproject.toml)
pycryptodomex==3.23.0
    # via steam
pygments==2.19.2
    # via rich
pynacl==1.6.2
    # via steam-manifest-decrypt (pyproject.toml)
pyperclip==1.11.0
    # via steam-manifest-decrypt (pyproject.toml)
pyqt6==6.10.2
    # via
    #   steam-manifest-decrypt (pyproject.toml)
    #   pyqt6-webengine
pyqt6-qt6==6.10.2
    # via pyqt6
pyqt6-sip==13.10.2
    # via
    #   pyqt6
    #   pyqt6-webengine
pyqt6-webengine==6.10.0
    # via steam-manifest-decrypt (pyproject.toml)
pyqt6-webengine-qt6==6.10.2
    # via pyqt6-webengine
    # via pyqt6
pysocks==1.7.1
    # via
    #   torpy
    #   urllib3
pywin32-ctypes==0.2.3
    # via
    #   keyring
pyyaml==6.0.3
    # via steam-manifest-decrypt (pyproject.toml)
requests==2.32.5
    # via steam
rich==13.9.4
    # via steam-manifest-decrypt (pyproject.toml)
selenium==4.36.0
    # via steam-manifest-decrypt (pyproject.toml)
# seleniumbase is intentionally NOT pinned here. Every release pins
# attrs/charset-normalizer/idna/requests/selenium/setuptools/trio/
# urllib3/wsproto to a different exact set than ours, so when the
# pip resolver pulls it in transitively it walks the version tree
# trying to find a match and either fails (ResolutionImpossible) or
# downgrades half our other deps. seleniumbase is only used by the
# SteamDB Cloudflare-bypass fallback in sff/manifest/depot_history.py
# and those call sites lazy-import + ImportError-guard. On Linux the
# AppImage build runs `pip install seleniumbase --no-deps` separately
# (see build_linux_appimage.sh); on Windows the user / build runner
# can do the same when SteamDB UC mode matters:
#     pip install seleniumbase --no-deps
# That keeps every other pin sacred and lets the GUI EXE build without
# the ResolutionImpossible loop that wrecked the 6.3.1 workflow build.
# seleniumbase>=4.32
#     # via steam-manifest-decrypt (pyproject.toml) — UC mode for SteamDB CF bypass
setuptools>=82.0.0
    # via
    #   zope-event
    # NOTE: was ==82.0.0 but seleniumbase >=4.47.2 wants setuptools>=82.0.1.
    # Loosen to >=82 so pip's resolver can pick whatever satisfies both.
six==1.17.0
    # via steam
sniffio==1.3.1
    # via
    #   anyio
    #   trio
sortedcontainers==2.4.0
    # via trio
# steam==1.4.4 has an overly conservative urllib3<2 constraint that conflicts with
# selenium 4.x (needs urllib3>=2.x). Steam only uses `requests` at runtime and
# works fine with urllib3 2.x. Install it AFTER pip install -r requirements.txt:
#   pip install steam==1.4.4 --no-deps
# All of steam's transitive deps (gevent, six, protobuf, etc.) are already above.
# steam==1.4.4
#     # via steam-manifest-decrypt (pyproject.toml)
torpy==1.1.6
    # via steam-manifest-decrypt (pyproject.toml)
tqdm==4.67.3
    # via steam-manifest-decrypt (pyproject.toml)
trio>=0.31.0
    # via
    #   selenium
    #   trio-websocket
    # NOTE: was ==0.31.0 but seleniumbase >=4.45 needs trio>=0.32.0,
    # >=4.47 needs >=0.33.0. selenium 4.36 caps at <1.0 so any 0.3x
    # works for us. Range gives the resolver room to pick.
trio-websocket==0.12.2
    # via selenium
typing-extensions==4.15.0
    # via
    #   anyio
    #   black
    #   exceptiongroup
    #   selenium
urllib3>=2.0,<2.6
    # via
    #   requests
    #   selenium
    # NOTE: was ==2.6.3 but seleniumbase pins urllib3<2.6 on every
    # release that supports python 3.12. Selenium 4.36 needs >=2.5.0,
    # so the working window is 2.5.x. steam==1.4.4 declares <2 in its
    # setup.py but only uses requests at runtime — works fine on 2.5.x.
    # Install steam with --no-deps (see steam comment above) to bypass
    # the constraint check.
vdf==3.4
    # via
    #   steam-manifest-decrypt (pyproject.toml)
    #   steam
wcwidth==0.6.0
    # via prompt-toolkit
websocket-client==1.9.0
    # via selenium
win10toast==0.9
    # via steam-manifest-decrypt (pyproject.toml)
wsproto==1.2.0
    # via trio-websocket
zipp==3.23.0
    # via importlib-metadata
zope-event==6.0
    # via gevent
zope-interface==8.0.1
    # via gevent
py7zr==0.22.0
rarfile>=4.2
curl_cffi>=0.7
    # via steam-manifest-decrypt (pyproject.toml) — curl_cffi Chrome impersonation for SteamDB CF bypass
zendriver>=0.15.0
    # SteamDB Cloudflare bypass — CDP-based browser automation (no navigator.webdriver)
google-auth>=2.29.0
    # Google Drive API — OAuth 2.0 token handling
google-auth-oauthlib>=1.2.0
    # Google Drive API — InstalledAppFlow for desktop OAuth
google-api-python-client>=2.126.0
    # Google Drive API — googleapiclient.discovery
pyinstaller>=6.6
    # PyInstaller — used by build_simple.bat / build_simple_gui.bat / .spec files
    # to package SteaMidra into a single EXE for Windows. CI workflows on
    # github (build_installer.bat) need this on the runner; flagging it
    # here so the runner image's pip install -r requirements.txt grabs it
    # alongside everything else. Pin >=6.6 so the steam==1.4.4 + selenium
    # urllib3 conflict doesn't drag a too-old PyInstaller in.
